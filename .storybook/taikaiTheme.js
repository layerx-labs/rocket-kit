import { create } from 'storybook/theming/create';
import taikaiLogo from '../static/images/taikai-logo.svg';

export default create({
  base: 'light',
  brandTitle: 'TAIKAI Design System',
  brandUrl: '.',
  brandImage: taikaiLogo,
});
